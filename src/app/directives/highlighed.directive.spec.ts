import { HighlighedDirective } from './highlighed.directive';

describe('HighlighedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighedDirective();
    expect(directive).toBeTruthy();
  });
});
